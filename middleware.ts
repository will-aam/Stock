// Lógica do middleware.ts: Este arquivo terá um código que intercepta as requisições. A lógica será:

// Verificar se o usuário está tentando acessar uma rota protegida (qualquer URL que comece com /dashboard, /inventory-query, etc.).

// Verificar se existe um "token" ou "cookie" de sessão que prove que o usuário está logado. (Ainda vamos criar a lógica de login, por enquanto podemos simular).

// Se ele tentar acessar uma rota protegida e não estiver logado, redirecione-o para a página /login.
